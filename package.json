{
  "name": "dina3d-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate-models && vite",
    "build": "npm run generate-models && vite build",
    "preview": "vite preview",
    "generate-models": "node build-models.js"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "@google/model-viewer": "^4.1.0",
    "puppeteer": "^23.11.1",
    "three": "^0.172.0"
  }
}
